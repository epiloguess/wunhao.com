---
import { getCollection } from "astro:content";

const allPosts = await getCollection(
  "docs",
  (post) => post.data.draft == false
);

import Layout from "../layouts/PostsLayout.astro";
---

<Layout title="Writing" posts={allPosts} />
