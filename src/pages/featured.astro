---
import { getCollection } from "astro:content";

import Layout from "../layouts/PostsLayout.astro";

const featuredPosts = await getCollection(
  "docs",
  (post) => post.data.draft == false && post.data.featured === true
);
---

<Layout title="Featured" posts={featuredPosts} />
